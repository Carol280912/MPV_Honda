const icons={home:'<path d="m3 10 9-7 9 7v10H3Z"/><path d="M9 20v-7h6v7"/>',services:'<path d="M14 4a6 6 0 0 0-7 7L3 17a3 3 0 0 0 4 4l6-6a6 6 0 0 0 7-7l-4 4-4-4 4-4Z"/>',bookings:'<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M7 3v4m10-4v4M3 11h18m-13 5h3"/>',wallet:'<path d="M20 7H5a2 2 0 0 1 0-4h13v4M3 5v14a2 2 0 0 0 2 2h15V7m0 5h-6v5h6"/>',more:'<path d="M4 6h16M4 12h16M4 18h16"/>',car:'<path d="m4 10 2-6h12l2 6m-17 0h18v9H3Zm2 9v2m14-2v2M6 14h2m8 0h2"/>',bell:'<path d="M5 17h14l-2-3V9a5 5 0 0 0-10 0v5Zm5 3h4M12 2v2"/>',shield:'<path d="m12 3 8 3v6c0 5-8 9-8 9S4 17 4 12V6Zm-4 9 3 3 5-6"/>',parts:'<path d="m12 3 9 5v9l-9 5-9-5V8Zm-9 5 9 5 9-5m-9 5v9M7 5l9 5"/>',help:'<path d="M7 3H3c0 10 8 18 18 18v-4l-5-2-2 2a16 16 0 0 1-7-7l2-2Z"/>',health:'<path d="M3 12h4l3-7 4 14 3-7h4"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',gift:'<rect x="3" y="9" width="18" height="4"/><path d="M5 13v8h14v-8M12 9v12m0-12C3 9 5 1 9 4Zm0 0c9 0 7-8 3-5Z"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',check:'<path d="m5 12 4 4L19 6"/>',doc:'<path d="M5 3h10l4 4v14H5Zm10 0v5h4M8 12h8m-8 4h6"/>'};
const icon=n=>`<span class="icon" aria-hidden="true"><svg viewBox="0 0 24 24">${icons[n]||icons.car}</svg></span>`;
const seed={vehicle:0,points:3850,voucher:false,quote:'Awaiting approval',paid:false,bookings:[{id:'AC-260915',vehicle:0,service:'Periodic maintenance',date:'2026-09-15',time:'10:30',branch:'Glenmarie',price:420,status:'Confirmed'}],ledger:[{label:'Periodic maintenance',date:'14 Aug 2026',points:420},{label:'Service voucher redeemed',date:'10 Jul 2026',points:-1250},{label:'Insurance renewal',date:'22 May 2026',points:800}],requests:[],feedback:[],name:'Carol',notifications:true};let state;try{state=JSON.parse(localStorage.getItem('autocare-v1'))||structuredClone(seed)}catch{state=structuredClone(seed)}
const vehicles=[{name:'Honda Civic 1.5 Turbo',plate:'ABC 1234',year:'2020',mileage:'86,420',next:'88,000',distance:'1,580',health:82},{name:'Honda City 1.5 V',plate:'WXY 8821',year:'2021',mileage:'42,300',next:'45,000',distance:'2,700',health:94}];const dealer={name:'Premier Auto Group',branch:'Glenmarie',branches:['Glenmarie','Petaling Jaya','Shah Alam']};let tab='upcoming',rating=0;
const save=()=>localStorage.setItem('autocare-v1',JSON.stringify(state));const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const money=n=>'RM'+Number(n).toFixed(2);const go=r=>{location.hash=r};const toast=s=>{const t=document.querySelector('#toast');t.textContent=s;t.style.display='block';clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>t.style.display='none',4000)};const btn=(label,action,cls='')=>`<button class="button ${cls}" ${action}>${label}</button>`;const today=()=>new Date().toLocaleDateString('en-CA');const active=()=>vehicles[state.vehicle];
const car=`<svg class="car" viewBox="0 0 550 230" role="img" aria-label="Silver sedan illustration"><defs><linearGradient id="paint" x2="0" y2="1"><stop stop-color="#fcfdfb"/><stop offset=".4" stop-color="#bdc5bf"/><stop offset=".55" stop-color="#eef0ed"/><stop offset="1" stop-color="#7b8983"/></linearGradient><linearGradient id="glass" x2="0" y2="1"><stop stop-color="#344a47"/><stop offset="1" stop-color="#71827b"/></linearGradient></defs><ellipse cx="280" cy="193" rx="235" ry="12" fill="#415748" opacity=".13"/><path d="M25 154 42 121l91-24 65-51q18-11 45-11h104q35 1 60 27l51 47 54 18q18 8 20 28l-3 29H29Z" fill="url(#paint)" stroke="#89948d" stroke-width="2"/><path d="m149 98 56-48h56v51Zm125-48h68q22 0 39 16l39 38-145-3Z" fill="url(#glass)" stroke="#67766e" stroke-width="3"/><path d="m277 107 3 68m-145-68-14 60m309-52 20 57M53 150l449 3" fill="none" stroke="#829086"/><path d="m30 139 68-11-13 18-58 10m445-34 39 13 5 14-44-8" fill="#fcf7dc"/><path d="M39 173h471" stroke="#63736a" stroke-width="5"/><path d="M291 116h25m-149-1h23" stroke="#687970" stroke-width="4" stroke-linecap="round"/><g fill="#242e29" stroke="#4e5a52" stroke-width="5"><circle cx="116" cy="174" r="37"/><circle cx="429" cy="174" r="37"/></g><g fill="#b9c2b9" stroke="#7c8c80" stroke-width="7"><circle cx="116" cy="174" r="24"/><circle cx="429" cy="174" r="24"/></g><g stroke="#4a5b4e" stroke-width="4"><path d="m116 151 0 46m-23-23h46m274 0h46m-23-23v46"/></g></svg>`;
function shell(content,route){let section=route.split('/')[0]||'home';if(['booking','confirmation'].includes(section))section='bookings';if(['repair','quote','payment','health','parts','insurance','accident'].includes(section))section='services';const nav=['home','services','bookings','wallet','more'];return `<aside class="sidebar"><div class="logo"><span class="brandmark">A</span>AutoCare<span>.</span></div><div class="dealer">${dealer.name}</div><nav class="nav" aria-label="Main navigation">${nav.map(n=>`<button onclick="go('${n}')" class="${section===n?'active':''}" ${section===n?'aria-current="page"':''}>${icon(n)}${n[0].toUpperCase()+n.slice(1)}</button>`).join('')}</nav><div class="sidefoot"><p>Your car. Our commitment.</p><small>Care that goes beyond warranty.</small><button class="link" onclick="go('contact')">Contact your dealership ↗</button></div></aside><header class="topbar"><span class="crumb">My ownership / <b>${esc(section[0].toUpperCase()+section.slice(1))}</b></span><div class="logo mobilelogo"><span class="brandmark">A</span>AutoCare<span>.</span></div><div class="topright"><span class="demo">DEMO EXPERIENCE</span><button aria-label="Notifications" onclick="go('notifications')">${icon('bell')}</button><button class="avatar" aria-label="My profile" onclick="go('profile')">CT</button></div></header><main class="main">${content}<footer class="footer"><span>© 2026 AutoCare · ${dealer.name}</span><span>Fictional dealer & customer data</span></footer></main>`}
function picker(){return `<div class="vehiclepick">${icon('car')}<div><select aria-label="Active vehicle" onchange="state.vehicle=Number(this.value);save();render()">${vehicles.map((v,i)=>`<option value="${i}" ${state.vehicle===i?'selected':''}>${v.name}</option>`).join('')}</select><div class="plate">${active().plate}</div></div></div>`}
function home(){const v=active(),appt=state.bookings.find(b=>b.vehicle===state.vehicle&&b.status==='Confirmed');return `<div class="greeting"><div><div class="eyebrow">Your ownership companion</div><h1>Good to see you, ${esc(state.name)}.</h1><p>A little care today. More confidence on the road.</p></div>${picker()}</div><div class="dashboard"><div class="stack"><section class="vehiclehero"><div class="eyebrow">MY GARAGE / ${v.year}</div><h2>${v.name}</h2><span class="tag">● After-warranty care</span>${car}<div class="meta"><div><small>Registration</small><strong>${v.plate}</strong></div><div><small>Current mileage</small><strong>${v.mileage} km</strong></div></div></section><section class="card attention">${icon('clock')}<div style="flex:1"><div class="eyebrow" style="color:#ae824f;margin-bottom:6px">A little attention goes a long way</div><h3>Your next service is coming up</h3><small>Due at ${v.next} km · About ${v.distance} km to go</small></div>${btn('Book service',`onclick="go('booking')"`)}</section><section><div class="sectionhead"><h2>How can we help?</h2><button class="link" onclick="go('services')">All services ↗</button></div><div class="services">${[['services','Service','booking'],['services','Repairs','repair'],['parts','Parts','parts'],['shield','Insurance','insurance'],['help','Accident help','accident'],['health','Vehicle health','health']].map(([i,n,r])=>`<button class="service" onclick="go('${r}')">${icon(i)}${n}</button>`).join('')}</div></section><section class="promo"><div class="eyebrow">Made for the road ahead</div><h2>Your warranty ends.<br>Our care doesn’t.</h2><p>Enjoy 10% off selected labour + 500 bonus points.</p><button class="link" onclick="go('offer')">Explore after-warranty care &nbsp; →</button></section><div class="help">${icon('shield')}<div><strong>Expert care. Familiar faces.</strong>Your authorised dealership, with you for every kilometre.</div></div></div><div class="stack"><section class="card loyalty"><div class="row"><span class="eyebrow" style="margin:0">Your loyalty, rewarded</span><span class="gold">✧ GOLD MEMBER</span></div><div class="points">${state.points.toLocaleString()} <span>points</span></div><small>More journeys. More possibilities.</small><div class="progress"><i style="width:${Math.min(100,state.points/4000*100)}%"></i></div><div class="row"><small>${Math.max(0,4000-state.points)} pts to next tier</small><small>4,000 pts</small></div><div class="rewardnotice">${icon('gift')}<span>${state.voucher?'RM50 service voucher ready':'Rewards for your next visit'}</span></div>${btn('Explore my rewards &nbsp; →',`onclick="go('wallet')"`,'secondary full')}</section><section class="card"><div class="row"><h2>Your next visit</h2>${appt?'<span class="tag">● Confirmed</span>':''}</div>${appt?appointment(appt,true):'<p class="empty">No upcoming appointments</p>'}<div class="row">${btn(appt?'View booking':'Book service',`onclick="go('${appt?'bookings':'booking'}')"`,'secondary full')}</div></section><section class="card"><div class="row"><h2>Vehicle health</h2><div class="healthscore">${v.health}<span>/100</span></div></div><small>Last inspection · 14 August 2026</small><div class="healthitems">${['Engine oil','Tyres','Brakes','Battery'].map((n,i)=>`<div class="row"><span>${n}</span><span class="${i===3&&state.vehicle===0?'warn':'success'}"><i class="dot" style="${i===3&&state.vehicle===0?'background:#bd8a4a':''}"></i>${i===3&&state.vehicle===0?'Check advised':'Good'}</span></div>`).join('')}</div><button class="link" onclick="go('health')">View inspection report &nbsp; →</button></section></div></div>`}
function appointment(b,compact=false){const d=new Date(b.date+'T12:00:00');return `<div class="appt"><div class="datebox"><small>${d.toLocaleDateString('en',{month:'short'}).toUpperCase()}</small><strong>${d.getDate()}</strong></div><div><h3>${esc(b.service)}</h3><p>${d.toLocaleDateString('en',{weekday:'long'})} · ${b.time}<br>${esc(b.branch)} branch${compact?'':` · ${vehicles[b.vehicle].plate}<br>Reference ${b.id} · ${money(b.price)} estimate`}</p></div></div>`}
function booking(id){const b=state.bookings.find(x=>x.id===id);return `<div class="page"><div class="eyebrow">${b?'Manage your appointment':'Your next step to a healthier car'}</div><h1>${b?'Reschedule your visit':'Book a service'}</h1><p>Clear pricing. Convenient appointments. Expert care for your ${active().name}.</p><form class="card" id="bookingForm"><div class="formgrid"><label>Service package<select name="service"><option value="420" ${!b||b.price===420?'selected':''}>Periodic maintenance — RM420</option><option value="180" ${b?.price===180?'selected':''}>Essential oil service — RM180</option><option value="80" ${b?.price===80?'selected':''}>Vehicle health inspection — RM80</option></select></label><label>Preferred branch<select name="branch">${dealer.branches.map(n=>`<option ${b?.branch===n?'selected':''}>${n}</option>`).join('')}</select></label><label>Date<input name="date" type="date" required min="${today()}" value="${b?.date||''}"></label><label>Arrival time<select name="time">${['09:00','10:30','14:00','15:30'].map(t=>`<option ${b?.time===t?'selected':''}>${t}</option>`).join('')}</select></label><label class="wide">Anything we should know? (optional)<textarea name="notes" maxlength="1000" placeholder="Tell us about your vehicle or visit.">${esc(b?.notes||'')}</textarea></label></div><div class="summary"><div class="row"><span>Vehicle</span><strong>${active().plate}</strong></div><div class="row"><span>Estimated package price</span><strong id="estimate">${money(b?.price||420)}</strong></div><small>Final work and invoice require your approval. Demo slots are illustrative.</small></div>${btn(b?'Confirm new appointment':'Confirm booking','type="submit"')}</form></div>`}
function bookings(){let list=state.bookings.filter(b=>tab==='upcoming'?b.status==='Confirmed':b.status!=='Confirmed');return `<div class="page"><div class="row"><h1>My bookings</h1>${btn('＋ Book service',`onclick="go('booking')"`)}</div><p>Your next visit, all in one place.</p><div class="tabs"><button class="${tab==='upcoming'?'selected':''}" onclick="tab='upcoming';render()">Upcoming</button><button class="${tab==='past'?'selected':''}" onclick="tab='past';render()">Past & cancelled</button></div><div class="list">${list.map(b=>`<section class="card"><div class="row"><span class="tag">${b.status}</span><small>${vehicles[b.vehicle].name}</small></div>${appointment(b)}${b.status==='Confirmed'?`<div class="actions">${btn('Reschedule',`onclick="state.vehicle=${b.vehicle};save();go('booking/${b.id}')"`,'secondary')}${btn('Add to calendar',`onclick="calendar('${b.id}')"`,'secondary')}${btn('Cancel booking',`onclick="cancelBooking('${b.id}')"`,'secondary')}</div>`:''}</section>`).join('')||'<div class="card empty">No appointments here yet.</div>'}</div></div>`}
function wallet(){return `<div class="page"><h1>My rewards</h1><p>A thank-you for every journey you take with us.</p><div class="card"><div class="row"><div><div class="eyebrow">Available balance</div><div class="amount">${state.points.toLocaleString()} <small>points</small></div></div><span class="gold">GOLD MEMBER</span></div><div class="notice">Demo programme: 1,250 points unlock one RM50 service voucher. Rewards have individual redemption rules.</div></div><div class="list"><section class="card"><div class="row"><div><h2>RM50 service voucher</h2><p>For service or repair invoices of RM100 or more.<br>1,250 points · One voucher per invoice</p></div>${btn(state.voucher?'Ready to use':'Redeem reward',`onclick="redeem()" ${state.voucher?'disabled':''}`)}</div></section><section class="card"><h2>Points activity</h2>${state.ledger.map(l=>`<div class="quoteitem row"><div><h3>${esc(l.label)}</h3><small>${esc(l.date)}</small></div><strong class="${l.points>0?'success':''}">${l.points>0?'+':''}${l.points.toLocaleString()}</strong></div>`).join('')}</section></div></div>`}
function repair(){return `<div class="page"><h1>Let’s get it checked.</h1><p>Tell your service advisor what’s happening with your ${active().name}.</p><form id="repairForm" class="card"><div class="formgrid"><label class="wide">What needs attention?<select name="category">${['Warning light','Engine','Air conditioning','Battery','Tyres','Brakes','Accident damage','Other'].map(n=>`<option>${n}</option>`).join('')}</select></label><label class="wide">Describe the problem<textarea name="description" required maxlength="2000" placeholder="When did it start? What have you noticed?"></textarea></label><label class="wide">Photos (optional, up to 5 MB each)<input name="photos" type="file" accept="image/jpeg,image/png,image/webp" multiple><small>Demo stores file names only. Files are not uploaded.</small></label></div><div class="actions">${btn('Request an inspection','type="submit"')}</div></form><div class="list"><div class="card"><div class="row"><div><h3>Brake repair quotation</h3><p>ABC 1234 · ${state.quote}</p></div>${btn('View quotation',`onclick="go('quote')"`,'secondary')}</div></div>${state.requests.map(r=>`<div class="card"><h3>${esc(r.category)} · Request saved</h3><p>${esc(r.description)}</p><small>Demo request ${r.id} · ${vehicles[r.vehicle].plate}</small></div>`).join('')}</div></div>`}
function quote(){return `<div class="page"><div class="eyebrow">QUO-2026-8831 · ABC 1234</div><h1>Your repair quotation</h1><p>Front brake pad replacement · Glenmarie branch</p><div class="card"><span class="tag">${state.quote}</span><div class="notice">Inspection finding: front brake pads require replacement. This is a fictional inspection and quotation.</div><div class="quoteitem row"><div><h3>Front brake pad set</h3><small>Parts · Quantity 1</small></div><strong>RM480.00</strong></div><div class="quoteitem row"><div><h3>Installation & inspection</h3><small>Labour</small></div><strong>RM120.00</strong></div>${state.quote==='Awaiting approval'?`<label class="check"><input type="checkbox" id="labour" checked onchange="quoteTotal()">Apply 10% labour promotion (−RM12)</label><label class="check"><input type="checkbox" id="voucher" ${state.voucher?'':'disabled'} onchange="quoteTotal()">Use RM50 service voucher ${state.voucher?'':'(redeem in Wallet first)'}</label><div class="summary row"><span>Total payable</span><strong class="amount" id="quoteTotal">RM588.00</strong></div><div class="actions">${btn('Approve repair',`onclick="approveQuote()"`)}${btn('Decline quotation',`onclick="go('decline')"`,'secondary')}</div>`:`<div class="summary row"><span>Approved total</span><strong>${money(state.quoteAmount||600)}</strong></div>${state.quote==='Approved'?btn(state.paid?'View receipt':'Review payment',`onclick="go('${state.paid?'documents':'payment'}')"`):''}`}</div></div>`}
function simple(title,body){return `<div class="page"><h1>${title}</h1><div class="list">${body}</div></div>`}
function services(){return simple('Care for every kilometre', [['Service booking','Choose a package and a convenient appointment.','booking','services'],['Repairs & quotations','Get help, review costs and approve work.','repair','services'],['Parts & installation','Request a part matched to your vehicle.','parts','parts'],['Insurance renewal','Keep your cover and renewal date in view.','insurance','shield'],['Accident assistance','Find help and record incident details.','accident','help'],['Vehicle health','Understand your latest inspection.','health','health']].map(([n,d,r,i])=>`<button class="card row" style="text-align:left" onclick="go('${r}')"><div class="row">${icon(i)}<div><h2>${n}</h2><p>${d}</p></div></div>${icon('arrow')}</button>`).join(''))}
function more(){return simple('Your ownership essentials',[['My profile','profile'],['My vehicles','vehicles'],['Service history','history'],['My documents','documents'],['Notifications','notifications'],['Feedback','feedback'],['Contact dealership','contact']].map(([n,r])=>`<button class="card row" onclick="go('${r}')"><h3>${n}</h3>${icon('arrow')}</button>`).join(''))}
function page(route,id){switch(route){case 'home':return home();case 'services':return services();case 'bookings':return bookings();case 'booking':return booking(id);case 'wallet':return wallet();case 'repair':return repair();case 'quote':return quote();case 'more':return more();case 'confirmation':{let b=state.bookings.find(x=>x.id===id);return simple('You’re booked in.',`<div class="card"><span class="tag">${icon('check')} Appointment confirmed</span>${b?appointment(b):''}<p>This demo booking is saved on this browser. It has not been sent to a dealership.</p>${btn('View my bookings',`onclick="go('bookings')"`)}</div>`)}case 'decline':return simple('Help us understand',`<form class="card" id="declineForm"><label>Reason<select name="reason">${['Price','Repair elsewhere','Need more time','Do not require repair','Other'].map(n=>`<option>${n}</option>`).join('')}</select></label><div class="actions">${btn('Confirm decline','type="submit"')}${btn('Back to quotation',`type="button" onclick="go('quote')"`,'secondary')}</div></form>`);case 'payment':return simple('Review your payment',`<div class="card"><div class="row"><h2>Brake repair · QUO-2026-8831</h2><strong>${money(state.quoteAmount||600)}</strong></div><div class="notice">Payment sandbox: no money will be collected. A successful simulation creates a demo receipt and awards points once.</div><label>Simulate payment outcome<select id="outcome"><option>Success</option><option>Failed</option><option>Pending</option><option>Cancelled</option></select></label><div class="actions">${btn(state.paid?'Already paid':'Simulate payment',`onclick="pay()" ${state.paid||state.quote!=='Approved'?'disabled':''}`)}</div></div>`);case 'health':return simple('Your vehicle health',`<div class="card"><div class="row"><h2>${active().name}</h2><span class="amount">${active().health}<small>/100</small></span></div><p>Dealer inspection · 14 August 2026 · Demo report</p>${['Engine oil','Tyres','Brakes','Battery','Coolant'].map((n,i)=>`<div class="quoteitem row"><h3>${n}</h3><span class="${i===3?'warn':'success'}">${i===3?'Check advised':'Good'}</span></div>`).join('')}<div class="actions">${btn('Book an inspection',`onclick="go('booking')"`)}${btn('Request quotation',`onclick="go('repair')"`,'secondary')}</div></div>`);case 'history':return simple('Service history',`<div class="card"><span class="tag">Completed</span><h2 style="margin-top:15px">Periodic maintenance</h2><p>14 August 2026 · ${active().plate} · Glenmarie<br>Oil and filter replacement, fluid checks and inspection.</p>${btn('View documents',`onclick="go('documents')"`,'secondary')}</div>${state.paid?'<div class="card"><h2>Brake pad replacement</h2><p>Demo transaction completed · ABC 1234</p></div>':''}`);case 'documents':return simple('My documents',`<div class="notice">Demo documents contain fictional data. Production documents will require authenticated, server-authorised access.</div>${[['Service record','Periodic maintenance — 14 August 2026'],['Repair quotation','QUO-2026-8831 — '+money(state.quoteAmount||600)],...(state.paid?[['Payment receipt','Paid — '+money(state.quoteAmount)]]:[])].map(([n,d])=>`<div class="card row"><div><h3>${n}</h3><p>${d}</p></div>${btn('Download',`onclick="downloadDoc('${n}')"`,'secondary')}</div>`).join('')}`);case 'profile':return simple('My profile',`<form id="profileForm" class="card"><label>Preferred name<input name="name" value="${esc(state.name)}" required maxlength="40"></label><label class="check"><input name="notifications" type="checkbox" ${state.notifications?'checked':''}>Receive promotional updates</label><div class="notice">You are using a fictional demo profile. Sign-in, OTP and secure account recovery are not connected yet.</div>${btn('Save preferences','type="submit"')}</form>`);case 'vehicles':return simple('My garage',vehicles.map((v,i)=>`<div class="card row"><div><h2>${v.name}</h2><p>${v.plate} · ${v.mileage} km</p></div>${btn(state.vehicle===i?'Active vehicle':'Select vehicle',`onclick="state.vehicle=${i};save();render()"`,state.vehicle===i?'':'secondary')}</div>`).join(''));case 'notifications':return simple('Your updates',`<div class="card"><h3>Your next service is approaching</h3><p>Plan ahead for your ${active().next} km service.</p>${btn('Book service',`onclick="go('booking')"`,'secondary')}</div><div class="card"><h3>Your repair quotation is ready</h3><p>Review the itemised costs and available rewards.</p>${btn('View quotation',`onclick="go('quote')"`,'secondary')}</div>`);case 'feedback':return simple('How was your visit?',`<form id="feedbackForm" class="card"><p>Your feedback helps us care for you better.</p><div class="rating" role="group" aria-label="Rating">${[1,2,3,4,5].map(n=>`<button type="button" aria-label="${n} star${n>1?'s':''}" class="${rating===n?'selected':''}" onclick="rating=${n};render()">${n}</button>`).join('')}</div><label style="margin-top:20px">Your feedback<textarea name="comments" maxlength="2000"></textarea></label>${rating&&rating<=3?'<label class="check"><input type="checkbox" name="contact">I would like the service manager to contact me</label>':''}<div class="actions">${btn('Submit feedback','type="submit"')}</div></form>`);case 'parts':return requestPage('Parts, matched to your car','Part required',['Battery','Brake pads','Oil filter','Wipers','Tyres','Accessories']);case 'insurance':return requestPage('Stay covered for the road ahead','Request type',['Insurance renewal quotation','Update policy information']);case 'accident':return simple('Accident assistance',`<div class="card"><h2>Get help when you need it</h2><p>Contact your roadside assistance provider or dealership using your policy or verified contact details.</p><div class="notice">Demo only. No live dispatch, location tracking or emergency phone number is configured.</div>${btn('View dealer contact',`onclick="go('contact')"`)}<div class="actions">${btn('Record accident details',`onclick="go('repair')"`,'secondary')}</div></div>`);case 'contact':return simple('Your dealership',`<div class="card"><h2>${dealer.name}</h2><p>Glenmarie · Petaling Jaya · Shah Alam</p><div class="notice">This is a fictional dealership. Verified phone numbers, addresses and opening hours will be added for the pilot.</div>${btn('Request dealer contact',`onclick="go('repair')"`,'secondary')}</div>`);case 'offer':return simple('Care beyond your warranty',`<div class="promo"><div class="eyebrow">After-warranty care</div><h2>10% off selected labour.<br>500 bonus points.</h2><p>A little extra value for your next visit.</p></div><div class="card"><h2>Offer details</h2><p>Illustrative campaign only. Eligibility, expiry, bonus posting and benefit combinations must be configured by the pilot dealer. This offer is not applied automatically.</p>${btn('Explore service packages',`onclick="go('booking')"`)}</div>`);default:return simple('Page not found',btn('Return home',`onclick="go('home')"`))}}
function requestPage(title,label,options){return simple(title,`<form id="genericForm" class="card"><label>${label}<select name="category">${options.map(n=>`<option>${n}</option>`).join('')}</select></label><label style="margin-top:20px">Details<textarea required name="description" maxlength="2000" placeholder="Tell us what you need."></textarea></label><div class="notice">Your request is saved in this browser for demonstration. It is not sent to a dealer or insurer.</div>${btn('Save request','type="submit"')}</form>`)}
function render(){const [route='home',id]=location.hash.slice(1).split('/');document.querySelector('#app').innerHTML=shell(page(route||'home',id),route||'home');document.title=`AutoCare · ${(route||'home').replace(/^./,x=>x.toUpperCase())}`;const form=document.querySelector('#bookingForm');if(form){form.service.onchange=()=>document.querySelector('#estimate').textContent=money(form.service.value);form.onsubmit=e=>{e.preventDefault();const f=new FormData(form);if(f.get('date')<today())return toast('Please choose today or a future date.');if(state.bookings.some(x=>x.id!==id&&x.status==='Confirmed'&&x.date===f.get('date')&&x.time===f.get('time')&&x.branch===f.get('branch')))return toast('That demo slot is already booked. Choose another time.');const b={id:id||'AC-'+Date.now().toString().slice(-8),vehicle:state.vehicle,service:form.service.selectedOptions[0].text.split(' — ')[0],price:Number(f.get('service')),date:f.get('date'),time:f.get('time'),branch:f.get('branch'),notes:f.get('notes'),status:'Confirmed'};state.bookings=state.bookings.filter(x=>x.id!==id);state.bookings.push(b);save();go('confirmation/'+b.id)}}const rf=document.querySelector('#repairForm');if(rf)rf.onsubmit=e=>{e.preventDefault();const f=new FormData(rf),files=[...rf.photos.files];if(files.some(x=>x.size>5*1024*1024||!['image/jpeg','image/png','image/webp'].includes(x.type)))return toast('Use JPEG, PNG or WebP images under 5 MB.');state.requests.unshift({id:Date.now(),vehicle:state.vehicle,category:f.get('category'),description:f.get('description'),files:files.map(x=>x.name)});save();render();toast('Demo inspection request saved.')};const gf=document.querySelector('#genericForm');if(gf)gf.onsubmit=e=>{e.preventDefault();const f=new FormData(gf);state.requests.unshift({id:Date.now(),vehicle:state.vehicle,category:f.get('category'),description:f.get('description')});save();toast('Demo request saved. Find it under Repairs.');gf.reset()};const pf=document.querySelector('#profileForm');if(pf)pf.onsubmit=e=>{e.preventDefault();state.name=pf.elements.name.value.trim()||'Carol';state.notifications=pf.notifications.checked;save();toast('Preferences saved.')};const df=document.querySelector('#declineForm');if(df)df.onsubmit=e=>{e.preventDefault();state.quote='Declined';state.declineReason=df.reason.value;save();go('quote')};const ff=document.querySelector('#feedbackForm');if(ff)ff.onsubmit=e=>{e.preventDefault();if(!rating)return toast('Please select a rating.');state.feedback.push({rating,comments:ff.comments.value,contact:!!ff.contact?.checked,date:new Date().toISOString()});save();toast('Thank you. Your demo feedback has been saved.');go('home')}}
function cancelBooking(id){if(!confirm('Cancel this appointment?'))return;state.bookings.find(b=>b.id===id).status='Cancelled';save();render();toast('Booking cancelled.')}
function redeem(){if(state.voucher)return;if(state.points<1250)return toast('You need 1,250 points for this reward.');state.points-=1250;state.voucher=true;state.ledger.unshift({label:'RM50 service voucher redeemed',date:today(),points:-1250});save();render();toast('Your RM50 service voucher is ready.')}
function quoteTotal(){const total=600-(document.querySelector('#labour')?.checked?12:0)-(document.querySelector('#voucher')?.checked&&state.voucher?50:0);document.querySelector('#quoteTotal').textContent=money(total);return total}
function approveQuote(){if(state.quote!=='Awaiting approval')return;state.quoteAmount=quoteTotal();state.usedVoucher=!!document.querySelector('#voucher')?.checked;state.quote='Approved';if(state.usedVoucher)state.voucher=false;save();render();toast('Repair approved. Your selected price is saved.')}
function pay(){if(state.paid||state.quote!=='Approved')return;const outcome=document.querySelector('#outcome').value;state.paymentStatus=outcome;if(outcome==='Success'){state.paid=true;const pts=Math.floor(state.quoteAmount);state.points+=pts;state.ledger.unshift({label:'Brake repair payment',date:today(),points:pts});save();toast('Demo payment confirmed. Receipt and points added.');go('documents')}else{save();toast(outcome==='Pending'?'Payment pending. No points have been awarded.':`Payment ${outcome.toLowerCase()}. You can try again.`)}}
function download(name,text,type='text/plain'){const u=URL.createObjectURL(new Blob([text],{type}));const a=document.createElement('a');a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
function downloadDoc(n){download(n.toLowerCase().replaceAll(' ','-')+'.txt',`AUTOCARE — DEMO ${n.toUpperCase()}\nFictional document. Not an official invoice or receipt.\nVehicle: ABC 1234\nDealer: Premier Auto Group\nQuotation: QUO-2026-8831\nAmount: ${money(state.quoteAmount||600)}\nPayment: ${state.paid?'Demo paid':'Unpaid'}\n`)}
function calendar(id){const b=state.bookings.find(x=>x.id===id);const start=b.date.replaceAll('-','')+'T'+b.time.replace(':','')+'00';download(b.id+'.ics',`BEGIN:VCALENDAR\r\nVERSION:2.0\r\nPRODID:-//AutoCare//Demo//EN\r\nBEGIN:VEVENT\r\nUID:${b.id}@autocare.demo\r\nDTSTAMP:${new Date().toISOString().replace(/[-:]/g,'').replace(/\.\d{3}/,'')}\r\nDTSTART;TZID=Asia/Kuala_Lumpur:${start}\r\nDURATION:PT1H\r\nSUMMARY:AutoCare service appointment (demo)\r\nLOCATION:${b.branch}\r\nEND:VEVENT\r\nEND:VCALENDAR\r\n`,'text/calendar')}
window.addEventListener('hashchange',()=>{render();window.scrollTo(0,0)});render();
